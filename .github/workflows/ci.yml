name: CI

on:
  push:
  pull_request:

jobs:
  validate-runbooks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate runbooks
        run: bash scripts/validate-runbooks.sh
