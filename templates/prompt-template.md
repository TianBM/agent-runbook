# Prompt 模板

```text
任务：<用一句话描述要完成的事>

边界：
- 只在 <路径或系统> 内操作。
- 不执行 <禁止动作>。

输入证据：
- <必须读取的文件或命令输出>
- <必须确认的授权或状态>

必须调用的 skill：
- <skill-name>：<调用原因>

执行要求：
1. 先说明目的、思路、预期结果。
2. 按 runbook 的执行流程操作。
3. 运行验证命令并保留输出摘要。
4. 遇到停止条件时停止并说明原因。

交付：
- 改动摘要。
- 验证命令与结果。
- 未完成项或风险。
```
